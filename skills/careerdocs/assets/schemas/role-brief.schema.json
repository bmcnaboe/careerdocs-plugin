{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/bmcnaboe/careerdocs-plugin/schemas/role-brief.schema.json#1.0.0",
  "title": "RoleBrief",
  "type": "object",
  "required": ["requirements", "recommended_positioning"],
  "additionalProperties": false,
  "properties": {
    "organization": { "type": "string" },
    "role": { "type": "string" },
    "seniority": { "type": "string" },
    "location": { "type": "string" },
    "source": { "type": ["object", "null"] },
    "requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "text", "kind", "keywords"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^req-[0-9]+$" },
          "text": { "type": "string", "minLength": 1 },
          "kind": { "enum": ["must", "nice"] },
          "keywords": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "keywords": { "type": "array", "items": { "type": "string" } },
    "recommended_positioning": { "enum": ["executive", "builder"] },
    "notes": { "type": "string" },
    "approach": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "positioning": { "enum": ["executive", "builder"] },
        "lead_evidence": { "type": "array", "items": { "type": "string" } },
        "compress": { "type": "array", "items": { "type": "string" } },
        "resume_pages": { "type": "integer", "minimum": 1 },
        "letter_length": { "enum": ["note", "page"] },
        "tone": { "type": "string" },
        "avoid": { "type": "array", "items": { "type": "string" } },
        "notes": { "type": "string" }
      }
    }
  }
}
